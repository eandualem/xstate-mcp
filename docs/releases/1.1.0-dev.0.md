# 1.1.0-dev.0 — development candidate

This version is unpublished. It is a working version for release infrastructure;
it does not assign a final version to the other revival changes awaiting review.

- Use package.json as the version source for MCP initialize and registry metadata.
- Validate current MCP Registry and Glama metadata; retire legacy Smithery YAML.
- Build and compare two npm archives, then exercise the installed CLI with a real
  XState actor and an MCP client in a clean consumer.
- Replace immediate Makefile publication with an explicit tagged release workflow,
  artifact checksums, Node consumer checks and npm trusted publishing.

Before a stable release, integrate the import-safe CLI/library boundary (#5),
maintained dependencies (#6), contract CI (#7), and the selected revival fixes.
Review their combined compatibility impact, choose an unused stable version,
and write final release notes for that integrated source commit.
